# hc-turbo

This repo is test repo for turbo modules for WeFitter SDKs

## Basic setup

```
cd app
yarn add ../RTNWeFitterHealthConnect
cd android
./gradlew generateCodegenArtifactsFromSchema
cd ..
yarn android
```
